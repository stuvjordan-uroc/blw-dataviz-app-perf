import type { Layout, SegmentGroups, Point } from "./types.ts";
export default function makePoints(layout: Layout, segmentGroups: SegmentGroups) {
  const outPoints = [] as Point[]
  //figure out the number of points.
}